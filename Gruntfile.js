module.exports = function (grunt) {
  'use strict';

  // Force use of Unix newlines
  grunt.util.linefeed = '\n';

  RegExp.quote = function (string) {
    return string.replace(/[-\\^$*+?.()|[\]{}]/g, '\\$&');
  };

  var referenceDocNotice =
    '$1\n\n'
    + '[//]: # DO NOT EDIT IT WILL BE OVERWRITTEN - copy of bootstrap documentation generated by grunt docs-copy-bootstrap-docs\n\n'
    + '{% callout info %}\n**Bootstrap Reference Documentation** \n'
    + 'This is a part of the reference documentation from <a href="http://getbootstrap.com">Bootstrap</a>. \n'
    + 'It is included here to demonstrate rendering with Material Design for Bootstrap default styling. \n'
    + 'See the <a href="/material-design/buttons">Material Design</a> section for more elements and customization options.\n'
    + '{% endcallout %}'
    + '\n\n$2'

  var fs = require('fs');
  var path = require('path');
  var glob = require('glob');
  var isTravis = require('is-travis');
  var npmShrinkwrap = require('npm-shrinkwrap');
  var mq4HoverShim = require('mq4-hover-shim');
  var autoprefixer = require('autoprefixer')({
    browsers: [
      //
      // Official browser support policy:
      // http://v4-alpha.getbootstrap.com/getting-started/browsers-devices/#supported-browsers
      //
      'Chrome >= 35', // Exact version number here is kinda arbitrary
      // Rather than using Autoprefixer's native "Firefox ESR" version specifier string,
      // we deliberately hardcode the number. This is to avoid unwittingly severely breaking the previous ESR in the event that:
      // (a) we happen to ship a new Bootstrap release soon after the release of a new ESR,
      //     such that folks haven't yet had a reasonable amount of time to upgrade; and
      // (b) the new ESR has unprefixed CSS properties/values whose absence would severely break webpages
      //     (e.g. `box-sizing`, as opposed to `background: linear-gradient(...)`).
      //     Since they've been unprefixed, Autoprefixer will stop prefixing them,
      //     thus causing them to not work in the previous ESR (where the prefixes were required).
      'Firefox >= 31', // Current Firefox Extended Support Release (ESR)
      // Note: Edge versions in Autoprefixer & Can I Use refer to the EdgeHTML rendering engine version,
      // NOT the Edge app version shown in Edge's "About" screen.
      // For example, at the time of writing, Edge 20 on an up-to-date system uses EdgeHTML 12.
      // See also https://github.com/Fyrd/caniuse/issues/1928
      'Edge >= 12',
      'Explorer >= 9',
      // Out of leniency, we prefix these 1 version further back than the official policy.
      'iOS >= 8',
      'Safari >= 8',
      // The following remain NOT officially supported, but we're lenient and include their prefixes to avoid severely breaking in them.
      'Android 2.3',
      'Android >= 4',
      'Opera >= 12'
    ]
  });

  // Project configuration.
  grunt.initConfig({

    // Metadata.
    pkg: grunt.file.readJSON('package.json'),
    banner: '/*!\n' +
    ' * Bootstrap Material Design v<%= pkg.version %> (<%= pkg.homepage %>)\n' +
    ' * Copyright 2014-<%= grunt.template.today("yyyy") %> <%= pkg.author %>\n' +
    ' * Licensed under MIT (https://github.com/FezVrasta/bootstrap-material-design/blob/master/LICENSE)\n' +
    ' */\n',

    // Task configuration.
    clean: {
      'dist-css': 'web/css',
      'dist-js': 'web/js',
    },

    sass: {
      options: {
        includePaths: ['scss', 'node_modules'],
        precision: 6,
        sourceComments: true,
        sourceMap: true,
        outputStyle: 'expanded'
      },
      core: {
        files: {
          'web/css/<%= pkg.name %>.css': 'app/Resources/public/scss/<%= pkg.name %>.scss'
        }
      },
      docs: {
        files: {
          'docs/dist/css/docs.css': 'docs/assets/scss/docs.scss'
        }
      }
    },

    eslint: {
      options: {
        configFile: 'app/Resources/public/js/.eslintrc'
      },
      target: ['app/Resources/public/js/src/*.js']
    },

    jscs: {
      options: {
        config: 'app/Resources/public/js/.jscsrc'
      },
      grunt: {
        src: ['Gruntfile.js', 'grunt/*.js']
      },
      core: {
        src: 'app/Resources/public/js/src/*.js'
      },
      test: {
        src: 'app/Resources/public/js/tests/unit/*.js'
      },
    },

    stamp: {
      options: {
        banner: '<%= banner %>\n'
      },
      core: {
        files: {
          src: 'web/app/Resources/public/js/*.js'
        }
      }
    },

    //concat: {
    //  options: {
    //    stripBanners: false,
    //    sourceMap: true
    //  },

    uglify: {
      options: {
        compress: {
          warnings: false
        },
        mangle: false,
        preserveComments: /^!|@preserve|@license|@cc_on/i
      },

      dist: {
        files: {
          'web/js/bootstrap-material-design.iife.min.js': 'web/js/bootstrap-material-design.iife.js',
          'web/js/bootstrap-material-design.umd.min.js': 'web/js/bootstrap-material-design.umd.js',
          'web/js/bootstrap-material-design.es6.min.js': 'web/js/bootstrap-material-design.es6.js'
        }
      }
    },

    qunit: {
      options: {
        inject: 'app/Resources/public/js/tests/unit/phantom.js'
      },
      files: 'app/Resources/public/js/tests/index.html'
    },

    // CSS build configuration
    scsslint: {
      options: {
        bundleExec: true,
        config: 'app/Resources/public/scss/.scss-lint.yml',
        reporterOutput: null
      },
      core: {
        src: ['app/Resources/public/scss/*.scss', '!app/Resources/public/scss/_normalize.scss']
      },
    },

    postcss: {
      core: {
        options: {
          map: true,
          processors: [
            mq4HoverShim.postprocessorFor({hoverSelectorPrefix: '.bs-true-hover '}),
            autoprefixer
          ]
        },
        src: 'web/css/*.css'
      },
    },

    cssmin: {
      options: {
        // TODO: disable `zeroUnits` optimization once clean-css 3.2 is released
        //    and then simplify the fix for https://github.com/twbs/bootstrap/issues/14837 accordingly
        compatibility: 'ie9',
        keepSpecialComments: '*',
        sourceMap: true,
        advanced: false
      },
      core: {
        files: [
          {
            expand: true,
            cwd: 'web/css',
            src: ['*.css', '!*.min.css'],
            dest: 'web/css',
            ext: '.min.css'
          }
        ]
      },
    },

    csscomb: {
      options: {
        config: 'app/Resources/public/scss/.csscomb.json'
      },
      dist: {
        expand: true,
        cwd: 'web/css/',
        src: ['*.css', '!*.min.css'],
        dest: 'web/css/'
      },
    },


    connect: {
      server: {
        options: {
          port: 3000,
          base: '.'
        }
      }
    },

    jekyll: {
      options: {
        bundleExec: true,
        config: '_config.yml',
        incremental: false
      },
      docs: {},
      github: {
        options: {
          //raw: 'github: true'
          raw: 'baseurl: "/bootstrap-material-design"'
        }
      }
    },

    htmllint: {
      options: {
        ignore: [
          'Element “img” is missing required attribute “src”.',
          'Attribute “autocomplete” is only allowed when the input type is “color”, “date”, “datetime”, “datetime-local”, “email”, “month”, “number”, “password”, “range”, “search”, “tel”, “text”, “time”, “url”, or “week”.',
          'Attribute “autocomplete” not allowed on element “button” at this point.',
          'Element “div” not allowed as child of element “progress” in this context. (Suppressing further errors from this subtree.)',
          'Consider using the “h1” element as a top-level heading only (all “h1” elements are treated as top-level headings by many screen readers and other tools).',
          'The “datetime” input type is not supported in all browsers. Please be sure to test, and consider using a polyfill.'
        ]
      },
      src: ['app/Resources/public/js/tests/visual/*.html']
    },

    watch: {
      corejs: {
        files: 'app/Resources/public/js/src/*.js',
        tasks: ['dist-js']
      },
      docsjs: { // watch both the source and docs js
        files: ['app/Resources/public/js/src/*.js'],
        tasks: ['docs-js']
      },
      core: {
        files: 'app/Resources/public/scss/**/*.scss',
        tasks: ['dist-css']
      },
    },


    exec: {
      npmUpdate: {
        command: 'npm update'
      },
      'rollup-docs-iife': {
        command: 'node_modules/rollup/bin/rollup -c grunt/rollup.docs.iife.config.js'
      },
      'rollup-iife': {
        command: 'node_modules/rollup/bin/rollup -c grunt/rollup.iife.config.js'
      },
      'rollup-umd': {
        command: 'node_modules/rollup/bin/rollup -c grunt/rollup.umd.config.js'
      },
      'rollup-es6': {
        command: 'node_modules/rollup/bin/rollup -c grunt/rollup.es6.config.js'
      }
    },

    compress: {
      main: {
        options: {
          archive: 'bootstrap-material-design-<%= pkg.version %>-dist.zip',
          mode: 'zip',
          level: 9,
          pretty: true
        },
        files: [
          {
            expand: true,
            cwd: 'web/',
            src: ['**'],
            dest: 'bootstrap-material-design-<%= pkg.version %>-dist'
          }
        ]
      }
    }

  });


  require('load-grunt-tasks')(grunt);
  require('time-grunt')(grunt);

  // Docs HTML validation task
  grunt.registerTask('validate-html', ['jekyll:docs', 'htmllint']);

  var runSubset = function (subset) {
    return !process.env.MDB_TEST || process.env.MDB_TEST === subset;
  };
  var isUndefOrNonZero = function (val) {
    return val === undefined || val !== '0';
  };

  // Test task.
  var testSubtasks = [];
  // Skip core tests if running a different subset of the test suite
  if (runSubset('core') &&
      // Skip core tests if this is a Savage build
    process.env.TRAVIS_REPO_SLUG !== 'twbs-savage/bootstrap') {
    testSubtasks = testSubtasks.concat(['dist-css', 'dist-js', 'test-scss', 'test-js', 'docs']);
  }
  // Skip HTML validation if running a different subset of the test suite
  if (runSubset('validate-html') &&
    isTravis &&
      // Skip HTML5 validator when [skip validator] is in the commit message
    isUndefOrNonZero(process.env.MDB_DO_VALIDATOR)) {
    testSubtasks.push('validate-html');
  }
  // Only run Sauce Labs tests if there's a Sauce access key
  if (typeof process.env.SAUCE_ACCESS_KEY !== 'undefined' &&
      // Skip Sauce if running a different subset of the test suite
    runSubset('sauce-js-unit') &&
      // Skip Sauce on Travis when [skip sauce] is in the commit message
    isUndefOrNonZero(process.env.MDB_DO_SAUCE)) {
    testSubtasks.push('babel:core');
    testSubtasks.push('connect');
    testSubtasks.push('saucelabs-qunit');
  }
  grunt.registerTask('test', testSubtasks);
  grunt.registerTask('test-js', ['eslint', 'jscs:test', 'jscs:grunt', 'qunit']);

  // JS distribution task.
  grunt.registerTask('dist-js', [
    'clean:dist-js',
    'eslint',
    'jscs:test',
    'exec:rollup-iife',
    'exec:rollup-umd',
    'exec:rollup-es6',
    'stamp',
  ]);
  grunt.registerTask('docs-js', [
    'clean:docs-dist-js',
    'copy:dist-to-docs', // ensure dist is present after cleaning
    'eslint',
    'jscs:docs',
    'exec:rollup-docs-iife',
    'uglify:docs',
    'uglify:docs-vendor'
  ]);


  grunt.registerTask('test-scss', ['scsslint:core']);

  grunt.registerTask('sass-compile', ['sass:core']);

  grunt.registerTask('dist-css', [
    'scsslint:core',
    'clean:dist-css',
    'sass-compile',
    'postcss:core',
    'csscomb:dist',
    'cssmin:core'
  ]);


  // Full distribution task.
  grunt.registerTask('dist', ['dist-css', 'dist-js']);

  // Default task.
  grunt.registerTask('default', ['test']);
  //------
  // Docs tasks

  // Independent task to be run when we are ready to sync the bootstrap repo's docs locally.
  //  Should be automated with no need for intervention (other than pulling the right bootstrap release locally)
  grunt.registerTask('docs-copy-bootstrap-docs', [
    'copy:bs-docs-js-vendor',
    'copy:bs-docs-scss',
    'copy:bs-docs-getting-started',
    'copy:bs-docs-components',
    'copy:bs-docs-content',
    'copy:bs-docs-plugins'
  ]);

  grunt.registerTask('docs', ['docs-css', 'docs-js']);
  //------

  //------
  // Release and publish
  grunt.registerTask('prep-release', [
    'dist', // all dist including docs
    'jekyll:github', // build site from scratch
    'compress' // compress zip
  ]);
  grunt.registerTask('publish', [
    'prep-release',   // build all including dist, docs, site
    'buildcontrol:pages' // push site
  ]);
  //------

  // Task for updating the cached npm packages used by the Travis build (which are controlled by test-infra/npm-shrinkwrap.json).
  // This task should be run and the updated file should be committed whenever Bootstrap's dependencies change.
  grunt.registerTask('update-shrinkwrap', ['exec:npmUpdate', '_update-shrinkwrap']);
  grunt.registerTask('_update-shrinkwrap', function () {
    var done = this.async();
    npmShrinkwrap({dev: true, dirname: __dirname}, function (err) {
      if (err) {
        grunt.fail.warn(err);
      }
      var dest = 'grunt/npm-shrinkwrap.json';
      fs.renameSync('npm-shrinkwrap.json', dest);
      grunt.log.writeln('File ' + dest.cyan + ' updated.');
      done();
    });
  });

  //grunt.registerTask('debug', function () {
  //  console.log('');
  //});
};
